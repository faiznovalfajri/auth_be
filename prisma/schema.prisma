generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

// Model User = Tabel users di database
model User {
  id          String      @id@default(uuid())
  name        String      @unique
  password    String
}
